file:
  /etc/nginx/app.conf:
    exists: true
    contains:
{{ range $name, $value := .Vars.presents }}
      - /^[[:blank:]]*{{$name}} {{$value}};$/
{{ end }}
{{ range .Vars.absents }}
      - "!/^[[:blank:]]*{{ . }}/"
{{ end }}

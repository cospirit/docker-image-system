# Prevents access to other php files
location ~ \.php$ {
    return 404;
}

# Disable assets logs
location ~* \.(jpg|jpeg|gif|png|css|js|map|ico|xml)$ {
    access_log off;
}

# Deny all . files
location ~ /\. {
    deny all;
}
